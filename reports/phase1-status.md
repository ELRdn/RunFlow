# Phase 1.0 実装・検証記録

2026-09-08更新。**現状形状の暫定1姿勢CFDは収束し、生ログの独立監査・入力辞書の二重再生成もPASS。形状忠実度の基準は未達のまま保留しており、Phase 1全体・科学的妥当性は未完了。** 以下は初回の形状停止から成功までの履歴を含む。

## 実装した経路

`runflow cfd prepare / run / report`、固定プロトコル、専用JSON Schemaを追加した。Phase 0の契約と禁止フラグは維持する。

入力SHA、1µm統合、退化面・重複面の除去、法線整合、必要時1回だけの1mm再メッシュ、閉曲面検査、双方向の保守的表面距離、投影面積、自己交差の検査をつなぐ。形状不合格ではソルバーを起動しない。

OpenFOAM Foundation14の`blockMesh → surfaceFeatures → decomposePar → snappyHexMesh → checkMesh → foamRun`を実装した。MPI4プロセス、段階・合計時間、RSS、出力容量を監視する。MPI子プロセスが別セッションへ移っても追跡し、終了を確認する。

力・残差・流量の判定、正式係数をnullにする失敗契約、非公開の比較画像・履歴図・HTML報告を追加した。未生成・未検証の結果はunavailableとして扱う。

## 検証の範囲

追加局所修正前の自動検証は235 tests PASS（2026-09-07）。入力・許容差・未確定設定・係数の符号と単位・未収束・タイムアウト・公開拒否に加え、バイナリVTKの符号・単位・欠損・非有限値と、重複リンクの除外を確認した。固定Blenderの実行テストでは、四角形の三角形化で生じる退化面、局所統合、未使用頂点、点だけの接続、空の表面を検証した。追加した全体監査では、キャッシュの改変検出、距離の面積重み付けと被覆範囲、断面、投影の重複・隙間内の別部品・明示的な精度格子を合成形状で検証した。局所比較では、三角形の箱による切断、面の向き・面積・元面IDの保持と、固定Blenderでの閉じた立方体の再メッシュ・距離測定を検証した。

全身比較では、4条件の全身入力一致、バイナリ座標保持、入力改変、開いた形状、薄い面・隙間の消失、面数予測、時間・RAM・コミット・容量の上限、子プロセスの最終終了確認を追加検証した。失敗した段階の途中JSONを完成値として扱わず、中断・描画失敗後も元の全体・条件別締切を維持する。Phase 0の実採用manifestの二重生成と円柱の公開用出力も再確認した。

| 項目 | 確認内容 |
|---|---|
| 既存Phase 0 | 既存92テストを維持。採用manifestからの設定SHA二重生成が一致し、採用時のSHAとも同じ |
| 公開出力の退行 | 基準円柱からsummary.jsonだけ生成。Phase 1の結果は公開コマンドが拒否 |
| 閉じた合成形状 | 固定Blenderで補修不要の形状を通し、双方向距離とsurfaceCheckを確認 |
| 開いた合成形状 | 1mm再メッシュ経路の動作と判定を確認 |
| 合成メッシュ | 229,544セル。標準checkMesh、物体level 5、後流の未完了細分化0を確認 |
| 合成ソルバー | 3反復の正常終了、初期時刻を含む4行の力、残差6成分、流入出流量を取得。未収束と判定 |
| 中断 | Windows子プロセス群とLinuxの別セッション子プロセスを短いテストで終了確認 |
| 実オグリ | 初回とユーザー指示による継続試験を別記録で保存。継続試験後、保存済み表面の距離検査で許容差超過を確認。面積・自己交差の全条件合格とCFD実行には未到達 |
| 可視化 | 比較画像と非公開レポートを保存。合成VTKの4並列出力と読み取り、メッシュ断面・圧力・速度・力・残差の5図を実機確認 |

合成ソルバーは出力形式を確認する3反復テストであり、収束や空力精度の承認ではない。合成ケースの追加`checkMesh -allGeometry`検査では凹セルの指摘を保存した。通常の実行ゲートは標準checkMeshであり、この指摘を空力精度が良好という意味へ読み替えない。

MPI起動の描画系ハードウェア検出待ちは、プロセス単位の`HWLOC_COMPONENTS=-gl`で解消した。Foundation14の分割境界と流量出力構文は、実際の合成ソルバーで確認して修正した。

VTKのASCII出力はWSLからWindowsドライブへの小刻みな書き込みで時間切れとなった。プロセス診断で各rankの約10万回のwriteと`p9_client_rpc`待機を確認し、標準のバイナリ出力へ変更した。不要な境界面・リンクを省き、読み取り側をbig-endian binary/ASCIIの両方へ対応させた。
同じ保存済み合成結果と90秒上限で、出力本体は3.873秒・正常終了を確認した（コピーした検証ケースでは4.649秒）。ソルバーの再実行はしていない。検証の詳細と図は`private/phase1-validation/vtk-fix/`に保存した。

## 非公開の引き渡し

実対象の数値、補修後形状、比較画像、入力ID、時間・メモリ・容量、具体的な停止理由は`private/phase1/smoke-001/REVIEW.md`、継続試験は`private/phase1/smoke-002/REVIEW.md`に保存する。退化面の再現用局所データ、距離の独立検証と図は`private/phase1-validation/degeneracy/`に保存する。過去の試験記録を上書きせず、許容差も変更していない。

継続試験では再メッシュ後の退化した三角形を局所統合で除去した。統合後に残った未使用頂点を面の接続不良と誤解しないよう、局所再現データと全体の頂点参照を検査して原因を確定し、処理を修正した。修正後の全形状再生成はしていない。

未使用頂点を参照しない保存済み三角形表面に対して、既定の保守的距離検査は合格しなかった。細分化を追加した診断では実測の許容差超過点を取得し、さらに全候補三角形をfloat64で走査する別実装で確認した。診断は継続試験の形状600秒の元の締切内で終了し、再メッシュやソルバーの再実行はしていない。正式な試験結果はBLOCKEDのまま、診断結果は別記録とする。

その後のユーザー指示により、保存済み原形状・候補の全体監査を別に実施した。全三角形の双方向距離、3方向の投影面積、6方向の外部可視点、投影上の隙間、選定断面と主要部位を確認した。大きな輪郭と主要部位は保持されている一方、細部の消失・隙間の部分的な閉塞が残る。全表面の最大差には外から直接見えない面も含まれ、外形の最大変形と同一視しない。数値・比較図・計算費用は非公開の`private/phase1-validation/global-shape-002/REVIEW.md`に保存した。

監査は入力SHAを保持した読み取り専用の診断であり、再メッシュ・CFD・許容差変更はしていない。投影計算の数値例外と修正後の実行も別ログに保存した。再利用手順と全キャラ展開で必要な部位情報は [形状監査](../docs/SHAPE_AUDIT.md) を参照。

続いてユーザー承認の局所解像度比較を実施した。3領域・4解像度・2種類の余白の候補を非公開に生成し、原形状と保存済み全身候補への距離・局所投影面積・隙間を比較した。局所の細かい再メッシュは実行できたが、表面の大幅な消失が起きたため補修として採用していない。2種類の余白で測定結果が一致しても、全身の同じ解像度の結果とは一致せず、局所の一部の点や隙間だけで改善を判断できないことを確認した。

数値・比較図・費用は `private/phase1-validation/local-voxel-001/REVIEW.md`、手順は [局所ボクセル比較](../docs/LOCAL_VOXEL.md)。元モデルへの接続、全身モデルの置換、CFD、許容差変更は実施していない。次の補修方式では元の面を保持し、切り出しによる体積判定の変化を避ける必要がある。

実対象のCFD用形状が合格するまでは、キャラクターの流場・Drag・Cd・CdA・全周期の所要時間は確定できない。次回は今回の監査を材料に、外部表面・重なった内部面・隙間の扱いを明文化し、局所補修の対象を決める。元の2mm・1%条件は維持しており、条件の変更を採用した記録はない。頂点統合だけで距離超過は解消せず、閉鎖性・自己交差を含む全検査も未完了である。

その後のPhase 1で3段階メッシュ感度、領域感度、8/16/32時刻の感度を確認する。手足が動く非定常計算はPhase 1.5。欠落`Gallop.CharaTransformProcessData`の役割と公式ゲームとの完全一致は未確認のまま。

手順・契約は [Phase 1 runbook](../docs/PHASE1.md)。公開・pushは今回実施していない。

## 全身ボクセル比較の上限付き試験

2026-09-07。固定版Blender・同じ共通入力による4解像度の試験を終了した。粗い2条件は予定した計測を完了。より細かい候補は形状・深度・断面・双方向の全身と局所距離を取得したが、精密な投影計算が段階の時間上限に達した。最も細かい条件は空きRAMの条件で生成途中に停止し、形状を取得していない。試験終了と全4条件の比較完了を分離する。

細かい候補では広い部位の消失を確認した。閉じていない共通入力と距離場の内外判定の組合せが原因候補として残る。固定版で演算直前の状態を再現し、スケール違い、未適用モディファイア、体積補正の混在は今回に該当しないことを確認した。境界の存在だけで根本原因を確定せず、単純な合成形状の成功だけで入力依存の実装上の問題を除外しない。

数値・形状・図・提出レポートとの照合・ハッシュ台帳は非公開の`E:\RunFlowPrivate\phase1-validation\fullbody-voxel-001\FINAL_REVIEW.md`と同ディレクトリに保存した。原本の未改変、処理コードの固定、設定ID、子プロセス終了、公開用出力からの排除を検証した。候補採用・CFD・許容差の変更は行っていない。手順と既知の制約は [全身ボクセル比較](../docs/FULLBODY_VOXEL.md)。

## 全身の中間幅4条件

2026-09-07。追加指示による0.9・0.8・0.7・0.6mmの全身比較を実施し、4条件とも生成・3方向の投影・全身と問題部位の双方向距離・隙間・断面・表面の構造検査を完了した。原本と共通入力、尺度、姿勢、Blender版を固定し、前回の参照形状を生成し直していない。粗い方の条件は前回の粗い候補に近い外形を示し、他の条件には広い部位の消失が残った。部位の人間レビュー、補修採用、科学的承認は未実施。

生成終了直後の監視警告は完了記録とSHA、プロセス終了を検証して別の集計に関連付けた。元の実行結果を成功へ書き換えず、未着手の計測だけを追加した。終了監視の修正、並行処理群のコミット合算と停止対象の分離、警告付き完成物とタイムアウト途中出力の区別、参照表示と新規費用の分離、検証後資料の上書き拒否を追加テストした。実行中の数値処理コードは変更せず、修正版は計測・集計の終了後に通常コードへ反映した。

入口は非公開の`E:\RunFlowPrivate\phase1-validation\fullbody-voxel-002\TRANSITION_REVIEW.md`。詳細は同じ保存先の`FINAL_REVIEW-verified.md`、`study-summary-verified.json`、検証後のハッシュ台帳。前回結果と今回の原集計・警告履歴は保持した。今回の4条件の計測完了は、CFD用形状の合格やPhase 1全体の完了を意味しない。キャラクターのCFD値と許容差は変更していない。

## 保存済み全身候補の修正比較

2026-09-07。ユーザーが選んだ1mm・0.9mmを使い、微小な最近点移動、法線方向の移動、薄い立体のExact結合、Manifoldによる結合、観測した空き空間の切り戻しを比較した。0.8mm以下も含め、新しいボクセル生成はしていない。生成可能だった候補は全身・問題領域の双方向距離、投影、可視点、選定断面、接続を計測した。全条件で合格する候補は得られず、CFDの停止を維持した。

面積が近くても欠落した凹部が戻るとは限らず、固体内部に入ってしまった元の可視面は和集合で足しても外面にならないことを実入力と合成形状で確認した。観測空間の切り戻しでは局所的に距離が改善したが、未修正の差が残る。独立した自己交差検査は候補によって結果が異なった。比較のため保存済み基準も検査し、元の検出箇所と補修後の検出箇所が同じとは限らないことを記録した。

Exact結合は急なメモリ確保で停止した。短い監視間隔でも設定上限を超えて確保されるため、追加方式にはOSのJob Objectによる割当て制限を実装し、制限超過を拒否する実機テストを追加した。WSLの自己交差検査は、ASCII中間出力をWSL内へ移し、同一のbinary STLと固定検査実行ファイルで完了を確認した。形状・許容差・検査本体は変更せず、最初の時間切れと途中成果物も保存した。

合成試験では、移動上限、閉じたパッチの和集合と内部面の除外、閉じた切削体と差集合の体積、読み取り専用binaryキャッシュ、STLの座標、部分計測の拒否、異なる候補の検査結果の拒否を検証した。設定IDには領域と確認点の設定も含め、原本全表面の判定を維持する。

保存済み基準の局所投影を参照する追加テストも通過した。領域と基準形状が同じでも、固定した投影SHAが変わっていれば参照を拒否する。

手順は [全身候補の微修正](../docs/FULLBODY_REPAIR.md)。形状・数値・検査・失敗履歴はEの非公開 `fullbody-repair-003` に保存済み。最終資料もユーザーによる手動コピーを経てEへ保存し、16ファイルのSHAとサイズを照合記録に対して再検証した。日本語レポートの入口は `E:\RunFlowPrivate\phase1-validation\fullbody-repair-003\final-review\final-REVIEW.md`。同じフォルダに比較図・統合結果・最終ハッシュ台帳・コピー照合記録を保存する。プロジェクト内の非公開の作成元、途中の報告、旧処理コード、失敗した方式を保持した。資料の引き渡しは完了したが、形状の全条件合格・科学的承認・人間の候補採用・CFDは未達のままで、公開・commit・pushは行っていない。

## 局所修正の残件調査

2026-09-07。実施済みの方式・設定・対象領域をコードと保存済み報告で確認し、CGAL、Blender、Manifold、libiglと関連論文を調査した。0.9mmを主軸、1mmを比較用とする [追加試験の計画](../docs/LOCAL_REPAIR_PLAN.md) を作成した。交差の発生工程を分離する記録、全身の欠陥台帳、多方向の切り戻し、元の面への張り替え、薄い部位の復元、制限付き位置補正、人間の選択を保存する修正を検討対象とする。

生成直後float64・完成時float32の保存状態は確認したが、丸めや簡略化が交差の原因だとはまだ確定していない。実測形状を追加生成せず、依存ツールの導入・CFD・許容差変更・科学的承認も行っていない。前回の計測完了を、すべての局所修正方式の検証完了としない。

## 承認後の局所修正試験

上記は計画作成時の状態。その後、ユーザーのGOを受けて専用CGAL補助プログラムと監視付き実行入口を追加し、保存済み0.9mmへの追加修正を承認された試行上限まで実施した。新しいボクセル再生成、CFD、許容差変更、候補採用は行っていない。

保存済み差集合出力からの診断再実行で、簡略化・丸め・再取り込みによる交差状態の変化を切り分けた。過去に保存されなかったメモリ内状態との一致は未検証である。新しい切り戻しでは全身簡略化とfloat32再取り込みを追加せず、連続面で原本を切らないことを検査した。

凹部と隙間の改善を確認し、選定候補の二重生成で設定・配列・コード・ツールの一致を検証した。全身・問題領域の双方向距離、正面・側面・上面の投影、可視面、断面、境界・交差・極小面を計測した。原本の深度図も再生成して一致を確認した。面積条件を満たしても、原本から候補への距離と可視面の反例、極小面の品質条件が残るため不合格を維持する。

張り替えと薄い部品の試行には、対応境界・面の向き・部品の交差による前提不成立がある。反復補正、明示的な向きの整理、接続部の再三角形化、未処理領域、人間による部位・境界選択は残件。試行枠の終了を、すべての局所修正を尽くしたという結論にはしない。

WSLでの新候補の独立検査と、開始時のEへの新規出力作成は自動承認レビューの時間切れで実施できなかった。検査器間の不一致を未確認として保持する。成果物はDの非公開 `local-repair-001` で作成し、凍結後の限定したコピーによりEの同名保存先への引き渡しと全台帳ファイルのSHA・サイズ照合を完了した。`E-copy-verification.json` と `finalization-guard.json` が保存・終了確認の記録である。実装・手順は [局所修正の実行記録](../docs/LOCAL_REPAIR_EXECUTION.md) を参照する。科学的承認・ランキング適格性・空力値の状態は変更しない。

追加実装後の最終自動検証は263 tests PASS（2026-09-07）。既存のPhase 0・公開用出力・CFD契約を含めて退行を確認した。比較画像を親エージェントが目視確認し、改善点と残存する欠けを併記した。画像確認だけで必須部位の保持を人間承認した扱いにはしていない。

## 現状形状による暫定CFDへの移行

ユーザーは形状の精度探索を止め、現在の候補で空気抵抗を測ってから改善の必要性を判断する方針を指定した。原本との形状差を保留した暫定CFDの契約・入力receipt・実行入口を追加した。数値条件・資源上限・収束判定は維持し、科学的状態を `UNVALIDATED_PROVISIONAL_GEOMETRY`、ランキング適格性をfalseとする。

最初の実試行は数値的な最小整理とCGALの閉鎖・交差検査を完了したが、OpenFOAMの表面検査の診断出力中に形状工程の上限へ達した。TIMEOUTを保存し、プロセス終了を確認した。キャラクターのメッシュ・CFDは未実行、正式なDrag/Cd/CdAはnull。詳しい費用・形状・ログは非公開の `private/phase1/provisional-001` に保存する。

導入済みFoundation14のソースで出力先を確認し、検査入力と作業場所をLinux内へ移す修正を行った。診断は一括コピー・ハッシュ照合し、一時領域も同じ時間・容量監視に含める。完成表面の再利用では13ファイルのSHAと前回の失敗記録を照合し、追加の補修や座標変更を行わない。実験の設定IDからローカル保存場所と実行ログを分離した。

修正後は278 tests PASS。合成物体による実OpenFOAMの検査・アーカイブ保存と、Linuxの時間上限・容量上限・子孫プロセス停止も確認した。実入力の追加試行は、前回の時間をリセットせず、新規予算の明示的な承認後に開始する。手順は [暫定CFD](../docs/PROVISIONAL_CFD.md)。

その後、追加1回・最大60分のユーザー承認を受けて `provisional-002` を実行した。同一の完成表面の再利用とLinux内の診断出力は成功し、OpenFOAM検査が最後まで到達した。一方、CGALとOpenFOAMの自己交差判定に不一致があり、形状検査でFAILとして停止した。資源上限による停止ではなく、メッシュ・ソルバーは未実行。前回記録、原本、候補形状、正式な空力値nullを保持した。

診断アーカイブの全体SHA、検出位置数とログの一致、子孫プロセスの終了、一時領域の整理を確認した。数値・3方向の検出位置図・日本語レビュー・ハッシュ台帳は非公開 `private/phase1/provisional-002/review` に保存した。検出位置だけでは実際の交差と数値判定の誤差を区別できず、次は同じ辺・面の組と読み込み座標を使った照合が必要。形状の追加精度探索、許容差変更、自動再試行、commit・pushは行っていない。

## 成功までの原因分析と再試行

上記の追加1回の終了後、ユーザーが成功まで分析・修正・再試行を継続するよう明示した。試行回数の制限を解除し、1回ごとの時間・資源上限と収束基準を維持する。

OpenFOAMと同じ経路の診断プログラムで読み込み後の全座標・面、検出した辺・面の組を取得した。全身CGAL入力とのSHA一致、stockの検出位置の全件再現を確認し、全検出組を正確な有理数で再計算した。今回の陽性は全件が非交差である。全身の厳密検査と組ごとの独立検査を合わせた検証記録を固定し、再利用時も入力・ツール・全件の検査を照合する。stockの生の判定は失敗のまま残し、候補を変形したり検査未完了を合格にしたりしていない。

その後のメッシュ生成では、参照辞書の境界面の歪み上限が最終のstock検査より緩いことを確認した。生成側の上限を厳しくした次の試行で、メッシュ品質と指定した細分化の検査を通過した。

初めて最後まで進んだキャラクターの定常計算は、力・流量収支が安定したが残差が横ばいとなり、未収束で終了した。正常終了・流場・履歴・子プロセスの停止を保存し、正式なDrag/Cd/CdAをnullに保った。標準SIMPLEの緩和設定でも振動が残り、速度の補間を局所的に制限する方式でも全残差は収束しなかった。原因と途中結果をそれぞれ保存し、数値拡散が大きい一次風上方式の比較へ進んだ。これは精度上の制約がある暫定試験であり、物理条件・形状・収束閾値を緩和したものではない。手順は [暫定CFD](../docs/PROVISIONAL_CFD.md)。

一次風上、速度緩和の変更、非直交補正、内側の線形解の高精度化、SIMPLECとの組み合わせでも横方向の残差が周期的に残った。保存流場の比較では局所的な流れの反転を確認し、力だけを根拠に定常収束を認定せず、非直交補正の制限を次の診断とした。この段階の正式な空力値はnull。

新しい初期化経路では、正常終了した未収束流場を、同じ形状・物性・メッシュを持つ次の試行へ引き継ぐ。全入力のハッシュ、開始時の力、再実行したメッシュ検査が実機で一致した。メッシュ生成ログの継承と実際の新規処理を区別し、元反復番号・通算反復数も保存する。各試行の上限を延長したり、過去の失敗を成功へ変更したりしていない。

追加診断で最大の局所変動を衣装の裾の先付近へ絞った。非直交補正の制限と乱流変数の更新係数変更でも振動は残り、圧力勾配をleastSquaresへ変更した試行は発散傾向を示したため棄却した。固定版omega壁関数の連続接続オプションも新規一様流場から比較したが、残差条件は満たさなかった。近壁モデル設定の変更と精度上の注意を明記し、導入済みC/Hソースも保存・ハッシュ照合した。この段階の自動検証は312 tests PASS、正式な空力値はnull。

## 暫定計測の成立と引き渡し

`provisional-016` は、診断した裾付近の空気側の格子を追加細分化し、新規一様流場から実行した。物体表面・速度・物性・SST・静止壁と全収束条件を保持した。壁関数は参照の切替方式へ戻し、数値反復は `standard-simple-upwind-damped-nonorth` を使用した。局所領域・元snapshot・追加levelを非公開プロトコルで明示し、格子生成の要求完了と各levelのセル数も照合した。

メッシュ品質、すべての初期残差の継続条件、抵抗の安定性、流量収支、正常終了がPASS。入力から集計まで各段階と合計の資源上限内で完了し、子プロセスの終了も確認した。正式な暫定Drag/Cd/CdAを保存し、過去の失敗・未収束の係数はnullのまま保持した。

保存した生ログを別の監査コードで読み、抵抗・係数・残差・流量・資源・ハッシュを独立に照合した。計算時の保存済みコードから入力辞書を2回生成し、全15ファイルが実入力とSHA一致。引き渡し時のコードでも同一入力を生成した。これはCFDの二重実行による再現性確認ではない。最終の自動検証は **323 tests PASS**（2026-09-08）。

結果・力と残差の履歴・流場断面・試行台帳・実測費用・独立監査・入力再生成・成果物SHAは非公開の `private/phase1/provisional-016/review/REVIEW.md` にまとめた。再実行手順は [暫定CFD](../docs/PROVISIONAL_CFD.md)。

一次風上の数値拡散、壁面解像度、形状忠実度とメッシュ・領域・位相サンプリング感度は未検証。局所格子での収束を空力精度の証明とは扱わない。科学的状態は `UNVALIDATED_PROVISIONAL_GEOMETRY`、ランキング対象外。追加試行は止め、暫定結果のレビューと今後の検証順序の判断へ引き渡す。
